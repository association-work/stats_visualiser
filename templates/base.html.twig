<!DOCTYPE html>
<html class="h-full bg-gray-100">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Public Data Analyzer{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body class="h-full">
        <div class="min-h-full">
            <nav class="bg-indigo-600">
                <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
                    <div class="flex h-16 items-center justify-between">
                        <div class="flex items-center">
                            <div class="flex-shrink-0">
                                <a href="{{ path('app_home') }}" class="text-white font-bold text-xl">PDA</a>
                            </div>
                            <div class="hidden md:block">
                                <div class="ml-10 flex items-baseline space-x-4">
                                    <a href="{{ path('app_home') }}" class="text-white hover:bg-indigo-500 rounded-md px-3 py-2 text-sm font-medium">Accueil</a>
                                    <a href="{{ path('app_import') }}" class="text-white hover:bg-indigo-500 rounded-md px-3 py-2 text-sm font-medium">Import des données</a>
                                    <a href="{{ path('app_diagnostic') }}" class="text-white hover:bg-indigo-500 rounded-md px-3 py-2 text-sm font-medium">Diagnostic</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </nav>
            
            <header class="bg-white shadow">
                <div class="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8">
                    <h1 class="text-3xl font-bold tracking-tight text-gray-900 text-center">Public Data Analyzer</h1>
                </div>
            </header>
            <main>
                <div class="mx-auto max-w-7xl py-6 sm:px-6 lg:px-8">
                    {% block body %}{% endblock %}
                </div>
            </main>
        </div>
    </body>
</html>